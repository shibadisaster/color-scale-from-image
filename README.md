# color-scale-from-image
 supposedly finds the polynomial that fits each rgb component (red, green, blue) from a color scale from an input image
 only reads points horizontally, rotate image if vertical
